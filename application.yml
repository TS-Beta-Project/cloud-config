spring:
  zipkin:
    base-url: http://ec2-18-228-81-105.sa-east-1.compute.amazonaws.com:9411/
  sleuth:
    sampler:
      probability: 1.0
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
  instance:
    hostname: ec2-18-229-162-130.sa-east-1.compute.amazonaws.com
    preferIpAddress: true
